# Build-time defaults for the deployed site. Everything here is inlined into
# the public JS bundle, so it must never hold a secret.
#
# There is exactly one: which endpoint the app talks to. Which model answers,
# which key pays and who may ask are the endpoint's business (see worker/).
VITE_PROXY_URL=https://caldrop-endpoint.sebastian-9fc.workers.dev/v1
